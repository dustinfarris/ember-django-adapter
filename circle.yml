machine:
  node:
    version: 8

dependencies:
  pre:
    - npm config set spin false
    - npm install -g yarn

  override:
    - yarn install

deployment:
  production:
    branch: master
    commands:
      - sudo pip install mkdocs
      - mkdocs gh-deploy

test:
  override:
    - yarn test
