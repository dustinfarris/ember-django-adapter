dependencies:
  override:
    - npm install -g bower
    - npm install
    - bower install

deployment:
  production:
    branch: master
    commands:
      - sudo pip install mkdocs
      - mkdocs gh-deploy
